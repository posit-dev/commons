# call_measure_tool errors for an unknown measure name

    Code
      call_measure_tool(list(), "nope", "{}")
    Condition
      Error in `call_measure_tool()`:
      ! No measure named "nope".
      i No measures are registered.

