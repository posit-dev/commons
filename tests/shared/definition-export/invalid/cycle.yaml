$version: "0.1.0"
$learn_more: https://data-dict.tidyverse.org/
tables:
  - name: orders
    columns:
      - name: amount
        type: number(quantity)
        range: [0, 1000]
    definitions:
      - name: chicken
        expr: egg AND amount > 0
      - name: egg
        expr: chicken AND amount < 10
