# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
    paths: [pkg-r/**, tests/shared/**, docs/**, .github/workflows/pkgdown.yaml]
  pull_request:
    paths: [pkg-r/**, tests/shared/**, docs/**, .github/workflows/pkgdown.yaml]
  release:
    types: [published]
  workflow_dispatch:

name: pkgdown.yaml

permissions: read-all

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pkg-r
    # Only restrict concurrency for non-PR jobs: those share gh-pages, and a
    # PR build deploys nothing. Races against py-docs.yaml's deploy are
    # handled at the push instead, by the deploy action's rebase.
    concurrency:
      group: pkgdown-${{ github.event_name != 'pull_request' || github.run_id }}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown, local::.
          needs: website
          working-directory: pkg-r

      - name: Build site
        run: pkgdown::build_site(new_process = FALSE, install = FALSE)
        shell: Rscript {0}

      - name: Deploy to GitHub pages 🚀
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@d92aa235d04922e8f08b40ce78cc5442fcfbfa2f # v4.8.0
        with:
          clean: false
          branch: gh-pages
          folder: docs
          # Both sites and the Python preview deploys push to this one
          # branch, so a deploy has to rebase onto whatever landed while it
          # was building. Forcing would silently drop that other deploy.
          force: false
          attempt-limit: 10
