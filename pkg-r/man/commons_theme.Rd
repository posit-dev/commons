% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{commons_theme}
\alias{commons_theme}
\title{bslib theme carrying the commons chat assets}
\usage{
commons_theme(..., preset = "shiny")
}
\arguments{
\item{...}{Named Sass variables forwarded to \code{\link[shinychat:page_chat_theme]{shinychat::page_chat_theme()}}.}

\item{preset}{A bslib or Bootswatch preset name.}
}
\value{
A \code{\link[bslib:bs_theme]{bslib::bs_theme()}} object.
}
\description{
\code{commons_theme()} starts from \code{\link[shinychat:page_chat_theme]{shinychat::page_chat_theme()}} and attaches
the commons chat stylesheet and JavaScript as an HTML dependency bundled
into the theme itself. Because the result is an ordinary
\code{\link[bslib:bs_theme]{bslib::bs_theme()}}, it works anywhere a bslib theme does: as the \code{theme}
argument of \code{\link[shinychat:page_chat]{shinychat::page_chat()}}, \code{\link[bslib:page_fillable]{bslib::page_fillable()}}, or any
other bslib page function.
}
\details{
Pair the theme with \code{\link[shinychat:chat_ui]{shinychat::chat_ui()}} (embedded in a bslib page) or
\code{\link[shinychat:page_chat]{shinychat::page_chat()}} on the UI side, and \code{\link[=commons_server]{commons_server()}} on the
server side.
}
\examples{
\dontrun{
library(shiny)
library(shinychat)

ui <- page_chat("Assistant", id = "chat", theme = commons_theme())

server <- function(input, output, session) {
  agent <- commons(
    ellmer::chat_anthropic(),
    data_sources = data_source(sales = sales)
  )
  commons_server("chat", agent)
}

shinyApp(ui, server)
}

}
