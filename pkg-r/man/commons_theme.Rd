% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{commons_theme}
\alias{commons_theme}
\title{Theme for custom commons chat apps}
\usage{
commons_theme(..., preset = "shiny")
}
\arguments{
\item{...}{Named Sass variables forwarded to
\code{\link[shinychat:page_chat_theme]{shinychat::page_chat_theme()}}.}

\item{preset}{A bslib or Bootswatch preset name.}
}
\value{
A \code{\link[bslib:bs_theme]{bslib::bs_theme()}} object.
}
\description{
\code{commons_theme()} is the building block for deploying a commons chat as a
Shiny app; for local development, use \code{\link[=commons_app]{commons_app()}}. Pair
\code{\link[shinychat:page_chat]{shinychat::page_chat()}} or \code{\link[shinychat:chat_ui]{shinychat::chat_ui()}} with
\code{\link[shinychat:chat_server]{shinychat::chat_server()}}, passing \code{theme = commons_theme()} so the
commons chat assets are on the page.
}
\details{
\code{commons_theme()} bundles the commons chat CSS and JavaScript into an
ordinary \code{\link[bslib:bs_theme]{bslib::bs_theme()}} (via \code{\link[shinychat:page_chat_theme]{shinychat::page_chat_theme()}}), so it
works anywhere a bslib theme does.
}
\section{Pre-warming}{

A \code{\link[=commons]{commons()}} agent builds its context index on first use. To move that
cost off the first question, call the agent's \code{prewarm()} method during
startup. \code{prewarm()} is synchronous and independent of the Shiny runtime,
so it can also warm the on-disk cache ahead of deployment; in a Shiny
server function, defer it to post-startup idle time so the index builds
while the user reads the welcome message:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{later::later(function() agent$prewarm())
}\if{html}{\out{</div>}}

\code{prewarm()} also starts a background process that downloads any uncached
pins into the local pins cache (see \code{\link[=data_source]{data_source()}}).
}

\examples{
\dontrun{
library(shiny)
library(shinychat)

ui <- page_chat("Assistant", id = "chat", theme = commons_theme())

server <- function(input, output, session) {
  # One agent per session, so each user gets their own agent state
  agent <- commons(
    ellmer::chat_anthropic(),
    data_sources = data_source(sales = sales)
  )
  # Build the context index during post-startup idle time
  later::later(function() agent$prewarm())
  chat_server("chat", client = agent)
}

shinyApp(ui, server)
}

}
