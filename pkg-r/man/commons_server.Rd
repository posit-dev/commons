% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R, R/theme.R
\name{commons_server}
\alias{commons_server}
\alias{commons_theme}
\title{Chat server and theme for custom commons apps}
\usage{
commons_server(id, client, ...)

commons_theme(..., preset = "shiny")
}
\arguments{
\item{id}{The ID of the chat element; must match the \code{id} of the
\code{\link[shinychat:page_chat]{shinychat::page_chat()}} or \code{\link[shinychat:chat_ui]{shinychat::chat_ui()}} on the page.}

\item{client}{A \code{\link[=commons]{commons()}} agent. In a deployed app, create the agent
inside the server function and pass it to \code{commons_server()} so each
Shiny session gets its own agent state.}

\item{...}{In \code{commons_server()}, arguments passed to
\code{\link[shinychat:chat_server]{shinychat::chat_server()}}. In \code{commons_theme()}, named Sass variables
forwarded to \code{\link[shinychat:page_chat_theme]{shinychat::page_chat_theme()}}.}

\item{preset}{A bslib or Bootswatch preset name.}
}
\value{
\code{commons_server()} returns the \code{\link[shinychat:chat_server]{shinychat::chat_server()}} result.
\code{commons_theme()} returns a \code{\link[bslib:bs_theme]{bslib::bs_theme()}} object.
}
\description{
These are the building blocks for deploying a commons chat as a Shiny
app; for local development, use \code{\link[=commons_app]{commons_app()}}. Pair \code{commons_server()}
with \code{\link[shinychat:page_chat]{shinychat::page_chat()}} or \code{\link[shinychat:chat_ui]{shinychat::chat_ui()}}, passing
\code{theme = commons_theme()} so the commons chat assets are on the page.
}
\details{
\code{commons_theme()} bundles the commons chat CSS and JavaScript into an
ordinary \code{\link[bslib:bs_theme]{bslib::bs_theme()}} (via \code{\link[shinychat:page_chat_theme]{shinychat::page_chat_theme()}}), so it
works anywhere a bslib theme does.
}
\examples{
\dontrun{
library(shiny)
library(shinychat)

ui <- page_chat("Assistant", id = "chat", theme = commons_theme())

server <- function(input, output, session) {
  # One agent per session, so each user gets their own agent state
  agent <- commons(
    ellmer::chat_anthropic(),
    data_sources = data_source(sales = sales)
  )
  commons_server("chat", agent)
}

shinyApp(ui, server)
}

}
