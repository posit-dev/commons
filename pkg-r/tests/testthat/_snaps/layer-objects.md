# layer print methods report their contents and return invisibly

    Code
      print(source_zero)
    Message
      A commons data source with 0 tables.
    Code
      print(source_one)
    Message
      A commons data source with 1 table.
    Code
      print(source_two)
    Message
      A commons data source with 2 tables.
    Code
      print(semantic_zero)
    Message
      A commons semantic layer with 0 measures.
    Code
      print(semantic_one)
    Message
      A commons semantic layer with 1 measure.
    Code
      print(semantic_two)
    Message
      A commons semantic layer with 2 measures.
    Code
      print(context_zero)
    Message
      A commons context layer with 0 documents.
    Code
      print(context_one)
    Message
      A commons context layer with 1 document.
    Code
      print(context_two)
    Message
      A commons context layer with 2 documents.

