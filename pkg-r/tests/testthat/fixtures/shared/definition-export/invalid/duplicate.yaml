$version: "0.1.0"
$learn_more: https://data-dict.tidyverse.org/
tables:
  - name: orders
    columns:
      - name: amount
        type: number(quantity)
        range: [0, 1000]
    definitions:
      - name: total
        expr: SUM(amount)
      - name: total
        expr: AVG(amount)
