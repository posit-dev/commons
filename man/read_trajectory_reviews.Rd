% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory-reviews.R
\name{read_trajectory_reviews}
\alias{read_trajectory_reviews}
\title{Read trajectory reviews}
\usage{
read_trajectory_reviews(
  review_file = Sys.getenv("COMMONS_REVIEW_FILE", unset = "commons-review.jsonl"),
  trajectories = NULL
)
}
\arguments{
\item{review_file}{Path to the review JSONL file. Defaults to the
\code{COMMONS_REVIEW_FILE} environment variable when set, and otherwise to
\code{"commons-review.jsonl"}.}

\item{trajectories}{An optional named list of conversations returned by
\code{\link[=read_trajectories]{read_trajectories()}}. When supplied, review records are joined to their
conversation or exchange.}
}
\value{
A list of actionable review records in file order: all notes and
currently active flags. When \code{trajectories} is supplied, each record also
contains \code{turns}.
}
\description{
\code{read_trajectory_reviews()} reads the append-only JSONL event log written by
\code{\link[=view_trajectories]{view_trajectories()}}. It returns every note and only the currently active
flags: superseded \code{flag} events and all \code{unflag} events are folded away.
}
\details{
When \code{trajectories} is supplied, each returned record gains a \code{turns} field
containing the reviewed conversation or question-and-answer exchange. This
makes the result ready for analysis by a person or coding agent. Records
whose trajectory is no longer available retain their logged question, tag,
source, and identifiers.

Review logs written before schema version 1 remain readable. Those legacy
records do not have source, user, or question metadata.
}
