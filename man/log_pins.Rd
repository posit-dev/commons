% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectories.R
\name{log_pins}
\alias{log_pins}
\alias{log_local}
\title{Configure where commons logs conversation trajectories}
\usage{
log_pins(share_with = NULL)

log_local(path = NULL)
}
\arguments{
\item{share_with}{An optional character vector of Connect usernames to grant
viewer access to logged trajectory pins. Requires the \pkg{connectapi}
package. Access is granted as each conversation's pin is created, so named
users retain ongoing access to new trajectories.}

\item{path}{A directory to write trajectory files to. Defaults to the
\code{COMMONS_LOG_DIR} environment variable, or a temporary directory.}
}
\value{
A logging spec to pass to the \code{log} argument of \code{\link[=commons]{commons()}}.
}
\description{
Pass one of these to the \code{log} argument of \code{\link[=commons]{commons()}} for control over where
trajectories are written. \code{log_pins()} writes to private Connect pins;
\code{log_local()} writes to a local directory.
}
